import 'package:flutter/material.dart';
class newscreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('mohammed',style: TextStyle(color: Colors.black,fontSize: 40.00),),),
    );
  }
}